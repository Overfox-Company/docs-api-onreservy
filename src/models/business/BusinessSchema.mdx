```typescript
{
    owner: string;
    name: string;
    workers: Worker[];
    products: Product_type[];
    category_products: string[];
    services: Service_type[];
    phone?: string;
    locations: Location[];
    avatar?: string;
    background?: string;
    description?: string;
    verified?: boolean;
    schedule: Schedule_type;
    createdAt?: Date;
    updatedAt?: Date;
}
